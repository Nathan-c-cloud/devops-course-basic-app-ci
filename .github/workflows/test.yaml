name : My workflow

run-name: ${{github.actor }} is testing out github actions
on: push

jobs:
  build :
    runs-on : [ubuntu-latest]
    steps :
      - run : echo "Welcome to my first workflows test with {{ github.event_name }}"